const add = (a,b)=> {
    return (a+b);
}
console.log(add(5,6));

//if and only if the paranthesis having the return function, then the statement can be written as follows.
const mul = (d,c) => (d*c);

console.log(mul(4,5));